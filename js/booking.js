// Виджет бронирования — Этап 3.
// Бэкенд уже существует: ../teacher-bot (Railway, FastAPI).
// Контракт API — ../teacher-bot/WEBSITE.md:
//   GET  /api/slots?days=14
//   GET  /api/my-lessons?name=...
//   POST /api/book
// Время в API — НСК; конвертация в МСК (−4 ч) — копировать mskHour()
// из teacher-bot/web/static/app.js.
